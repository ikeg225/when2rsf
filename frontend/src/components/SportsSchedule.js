import styles from './styles/SportsSchedule.module.css'

const upcomingEvents = {
    'date here': 'event'
}

export default function SportsSchedule() {
    // get the current data
    // use the variable to show next 3 upcoming events 

    return (
        <div className={styles.sportsSchedule}>
            <p>hi</p>
        </div>
    )
}